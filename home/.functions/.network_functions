function scanports {
  for i in {1..65535};
  do
    (echo < /dev/tcp/$1/$i) &>/dev/null && printf "\n[+] Open Port at\n: \t%d\n" "$i" || printf ".";
  done
}

function nwgetmac {
  ifconfig en0 | grep ether | cut -d' ' -f 2
}
